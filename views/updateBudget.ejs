<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Update Budget</title>
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Budget Tracker</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/dashboard">Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Main content -->
  <div class="container mt-4">
    <h2 class="text-center mb-4">Update Budget</h2>

    <% if (budget) { %>
      <form action="/updateBudget/<%= budget.budgetId %>" method="POST" class="mx-auto" style="max-width: 500px;">
        <div class="mb-3">
          <label for="category" class="form-label">Category:</label>
          <select class="form-control" id="category" name="category" required>
            <option value="Rent" <%=budget.category==="Rent" ? "selected" : "" %>>Rent</option>
            <option value="Transport" <%=budget.category==="Transport" ? "selected" : "" %>>Transport</option>
            <option value="Food" <%=budget.category==="Food" ? "selected" : "" %>>Food</option>
            <option value="Utilities" <%=budget.category==="Utilities" ? "selected" : "" %>>Utilities</option>
            <option value="Insurance" <%=budget.category==="Insurance" ? "selected" : "" %>>Insurance</option>
            <option value="Medical" <%=budget.category==="Medical" ? "selected" : "" %>>Medical</option>
            <option value="Saving, Investing, & Debt Payments" <%=budget.category==="Saving, Investing, & Debt Payments"
              ? "selected" : "" %>>Saving, Investing, & Debt Payments</option>
            <option value="Personal Spending & Entertainment" <%=budget.category==="Personal Spending & Entertainment"
              ? "selected" : "" %>>Personal Spending & Entertainment</option>
            <option value="Miscellaneous" <%=budget.category==="Miscellaneous" ? "selected" : "" %>>Miscellaneous
            </option>
          </select>
        </div>

        <div class="mb-3">
          <label for="month" class="form-label">Month:</label>
          <input type="month" class="form-control" id="month" name="month"
            value="<%= budget.month.toISOString().slice(0, 7) %>" required>
        </div>

        <div class="mb-3">
          <label for="amount" class="form-label">Amount ($):</label>
          <input type="number" class="form-control" id="amount" name="amount" value="<%= budget.amount %>" required>
        </div>

        <div class="d-grid">
          <button type="submit" class="btn btn-primary">Update Budget</button>
        </div>
      </form>
      <% } else { %>
        <p class="text-center">No budget was found.</p>
        <% } %>
  </div>
</body>

</html>